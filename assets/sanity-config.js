// Filled in once FFI's Sanity project exists (see Task 6). Everything else
// in this codebase imports from here — this is the only file that needs
// editing when the real project ID is known.
export const SANITY_PROJECT_ID = '6x1ga0me';
export const SANITY_DATASET = 'production';
export const SANITY_API_VERSION = '2024-01-01';
