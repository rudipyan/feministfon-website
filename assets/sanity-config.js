// FFI's Sanity project config. Everything else in this codebase imports
// from here — this is the only file that needs editing if the project ID,
// dataset, or API version ever changes.
export const SANITY_PROJECT_ID = '6x1ga0me';
export const SANITY_DATASET = 'production';
export const SANITY_API_VERSION = '2024-01-01';
